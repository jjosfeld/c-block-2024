#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int levenshtein(char * wort1, char* wort2){
	int len1 = strlen(wort1), len2 = strlen(wort2), distance;

	
	

	free(D);
	return distance;
}

int main(){
	printf("%d\n", levenshtein("Tier", "Tor"));
}